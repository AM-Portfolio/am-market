environment: prod

replicaCount: 1

resources:
  limits:
    cpu: 1000m
    memory: 1024Mi
  requests:
    cpu: 500m
    memory: 512Mi

# Vault Configuration (Prod Override)
vault:
  secretPaths:
    mongodb:
      path: "secret/data/prod/infra/mongodb"
    together:
      path: "secret/data/prod/api/together"

env:
  ENVIRONMENT: "prod"
  MONGO_DB: "mutual_funds"
