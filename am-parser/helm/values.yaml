global:
  image:
    registry: ghcr.io/AM-Portfolio
    tag: latest

replicaCount: 1

image:
  repository: am-parser
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 8000

resources: {}

environment: default
