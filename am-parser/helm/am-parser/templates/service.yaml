apiVersion: v1
kind: Service
metadata:
  name: {{ include "am-parser.fullname" . }}
  labels:
    app: {{ include "am-parser.name" . }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ include "am-parser.name" . }}
