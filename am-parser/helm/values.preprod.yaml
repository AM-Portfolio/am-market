environment: preprod

replicaCount: 2

resources:
  limits:
    cpu: 500m
    memory: 512Mi
  requests:
    cpu: 250m
    memory: 256Mi

# Vault Configuration
vault:
  enabled: true
  role: "am-parser-role"
  secretPaths:
    mongodb: "kv/preprod/database/mongo"
