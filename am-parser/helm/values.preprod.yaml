env:
  ENVIRONMENT: "preprod"

environment: preprod

probes:
  port: 8000
  liveness:
    path: /health
  readiness:
    path: /health

replicaCount: 2

resources:
  limits:
    cpu: 500m
    memory: 512Mi
  requests:
    cpu: 250m
    memory: 256Mi

# Vault Configuration
vault:
  enabled: true