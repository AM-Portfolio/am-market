# Golden fixtures

Committed JSON samples for Postman / regression. No secrets.

| File | When to fill |
|------|----------------|
| `v1-holdings-niftybees.json` | After Module 1 — frozen `POST /v1/etf/holdings` sample |
| `v2-holdings-niftybees.json` | After Module 1 — `POST /v2/funds/holdings` with nullable returns |
| `v2-performance-batch-sample.json` | After Module 1 — `POST /v2/funds/performance/batch` shape |

Replace stubs below with live preprod (or local) responses once APIs exist. Assert rules: PLAN *Postman accuracy contract*.
