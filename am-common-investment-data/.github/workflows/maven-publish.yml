# This workflow has been moved to:
# am-market/.github/workflows/am-common-investment-data-ci.yml
#
# It was consolidated there so all CI/CD for am-market lives in one place.
# When am-common-investment-data is extracted to its own repository:
#   1. Copy am-market/.github/workflows/am-common-investment-data-ci.yml
#      to <new-repo>/.github/workflows/ci.yml
#   2. Remove the `working-directory: am-common-investment-data` default
#   3. Remove the `paths:` filter from the `on:` trigger
#   4. Delete this placeholder file
