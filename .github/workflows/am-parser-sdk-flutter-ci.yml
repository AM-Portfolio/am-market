name: am-flutter-parser-sdk CI

on:
  push:
    paths:
      - 'am-market-sdk/flutter-parser-sdk/**'
  pull_request:
    paths:
      - 'am-market-sdk/flutter-parser-sdk/**'

jobs:
  build_and_verify_flutter_parser:
    permissions:
      contents: read
      packages: write
    uses: AM-Portfolio/am-pipelines/.github/workflows/central-sdk-publish.yml@main
    with:
      language: 'flutter'
      working_directory: 'am-market-sdk/flutter-parser-sdk'
    secrets: inherit
