name: am-parser CI

on:
  push:
    branches: [ "main" ]
    paths:
      - 'am-parser/**'
  pull_request:
    branches: [ "main" ]
    paths:
      - 'am-parser/**'

jobs:
  build_and_publish:
    uses: AM-Portfolio/am-cicd/.github/workflows/central-build-publish.yml@main
    with:
      language: 'python'
      working_directory: 'am-parser'
      image_name: 'am-parser'
